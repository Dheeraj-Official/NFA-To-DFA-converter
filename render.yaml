services:
  - type: web
    name: nfa-dfa-api
    env: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    plan: free
    envVars:
      - key: PORT
        value: 5000
      - key: ALLOWED_ORIGINS
        value: "*"
    healthCheckPath: /
